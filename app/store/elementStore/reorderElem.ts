import type { CanvasElem } from "~/types";
import { findElemAndContainer } from "./utils";

/**what do i want, when this button is clicked i want to move the active elem upwards in its parent..to do this i need two things, the paren id and the elems id */

export const reorderElem = {
	moveUp(parentId: CanvasElem, activeElem: string) {},
	moveDown() {},
};
